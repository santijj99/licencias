# API Licencias → Postgres compartido athland-postgres
SPRING_PROFILES_ACTIVE=prod
SERVER_PORT=8080
LICENCIAS_PUBLISH_PORT=8080
LICENCIAS_DB=licencias

# Credenciales del Postgres compartido (/opt/apps/postgres/.env)
DB_USERNAME=athland
DB_PASSWORD=CAMBIAR_password_fuerte_postgres
POSTGRES_PASSWORD=CAMBIAR_password_fuerte_postgres

JWT_SECRET=CAMBIAR_jwt_secret_minimo_32_caracteres_seguros_aqui!!
AES_KEY=0123456789abcdef0123456789abcdef

CORS_ORIGINS=http://localhost:5000,http://127.0.0.1:5000

JAVA_OPTS=-Xms128m -Xmx512m -XX:+UseG1GC
