services:
  postgres:
    image: postgres:16-alpine
    container_name: licencias-postgres
    environment:
      POSTGRES_DB: licencias
      POSTGRES_USER: licencias
      POSTGRES_PASSWORD: licencias
    ports:
      - "5432:5432"
    volumes:
      - licencias_pgdata:/var/lib/postgresql/data
    healthcheck:
      test: ["CMD-SHELL", "pg_isready -U licencias -d licencias"]
      interval: 10s
      timeout: 5s
      retries: 5

volumes:
  licencias_pgdata:
